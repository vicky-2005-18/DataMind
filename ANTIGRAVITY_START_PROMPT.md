# Paste this prompt into Antigravity agent chat

The following is an agent instruction, not a terminal command. Paste all text below the divider.

---

You are implementing my academic project:

**DataMind: An Interactive Machine Learning Discovery & Experimentation Platform.**

I have opened the project planning pack as the workspace root. First inspect the actual files. Read:

1. START_HERE.md
2. AGENTS.md
3. PRD.md
4. ARCHITECTURE.md
5. ROADMAP.md
6. FILE_STRUCTURE.md
7. TASKS.md
8. PROJECT_STATE.md
9. docs/ML_SPEC.md
10. docs/DATA_CONTRACTS.md
11. docs/DATABASE.md and docs/schema.sql
12. docs/SERVICE_CONTRACTS.md
13. docs/UI_SPEC.md
14. docs/TEST_PLAN.md
15. docs/SETUP_WINDOWS.md

Treat these as the implementation specification. This project is a working local ML application, with dataset exploration, real supervised model experiments, saved results and prediction. Full v1 later includes K-Means, explanations, exports and interactive algorithm discovery.

Use Python + Streamlit + pandas/NumPy + scikit-learn + Plotly + SQLite. Target Windows and CPU execution. No API key is required for DataMind core runtime. Do not replace the selected architecture with React, FastAPI, cloud services or an LLM chatbot.

First briefly explain your understanding, the planned module boundaries, and any material contradictions you find. Then implement **M0 — Foundation** from ROADMAP. Do not stop after producing another plan.

M0 deliverables:

- Working Python package and Streamlit entry point with page navigation.
- Typed settings, contracts and error structure, introduced only as needed.
- SQLite migration runner using the supplied schema specification.
- Local project create/list behavior with persistence across restart.
- Storage initialization and honest empty states for future pages.
- Dependency definitions and exact resolved versions from a verified environment; do not invent a tested lockfile.
- Small tests for migration idempotency and project persistence, plus app launch verification.
- Implementation README with commands actually verified in this environment.

Preserve all specification files. Inspect any existing implementation and continue it safely instead of overwriting blindly. Follow the environment's permissions and report genuine installation/network blockers. Ordinary authorized local work does not need repeated permission questions.

Use realistic defaults from the pack. Do not fabricate datasets, scores, screenshots or pass counts. Sample data must be generated by the specified demo loaders and clearly labeled. Do not train on holdout data or fit preprocessing before splitting when later ML milestones are implemented.

When M0 is verified, update TASKS.md and PROJECT_STATE.md. Finish with:

1. What now works.
2. Important files created/changed.
3. Commands and tests actually run, with results.
4. How I launch and manually verify M0.
5. Any remaining defect/blocker.
6. The next milestone prompt to use.

Complete M0 first. I will use the milestone prompts to continue the rest of the project in reviewable stages.
